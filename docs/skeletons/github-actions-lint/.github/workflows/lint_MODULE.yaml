---
# template-gh-lint: 1.1.0
name: "Lint / X_TARGET_TITLE_X"

env:
  DEV_CICD_DEBUG: ""
  BL64_LIB_CICD: "YES"

on:
  pull_request:
    branches:
      - "main"
  push:
    branches:
      - "feature/**"
    paths:
      - "src/**"

jobs:
  lint-X_TARGET_X:
    runs-on: "ubuntu-latest"
    steps:

      - name: "Check out repository code"
        uses: "actions/checkout@main"

      - name: "Prepare environment"
        run: "./bin/dev-lib-base"

      - name: "Download tools"
        run : "X_TOOLS_PLACEHOLDER_X"

      # - name: "Prepare for testing"
      #   run : "X_BUILD_PLACEHOLDER_X"

      - name: "Run Linter"
        run: "X_PLACEHOLDER_3_X"
...
