# delete-me # Add to the CICD components section of the dot.env file

# Lint / Dockerfile / Configuration
DEV_LIB_HADOLINT_CFG="${DEV_PATH_ROOT}/.hadolint.yaml"
export DEV_LIB_HADOLINT_CFG

# Lint / Dockerfile / Sources
DEV_CICD_LINT_DOCKERFILE_SOURCE="$DEV_PATH_SRC"
export DEV_CICD_LINT_DOCKERFILE_SOURCE
