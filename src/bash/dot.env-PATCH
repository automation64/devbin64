# delete-me # Add to the CICD components section of the dot.env file

# Build / Target file name
DEV_BUILD_NAME='X_BUILD_FILE_NAME_X'
export DEV_BUILD_NAME

# Build / Source full path
DEV_BUILD_SOURCE="${DEV_SRC}"
export DEV_BUILD_SOURCE

# Build / Target full path
DEV_BUILD_TARGET="${DEV_BUILD}/${DEV_BUILD_NAME}"
export DEV_BUILD_TARGET
