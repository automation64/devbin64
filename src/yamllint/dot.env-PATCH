# delete-me # Add to the CICD components section of the dot.env file

# Lint / YAML / Target
DEV_CICD_LINT_YAML_TARGET="$DEV_SRC"
export DEV_CICD_LINT_YAML_TARGET

# Lint / YAML / Configuration
DEV_LIB_YAMLLINT_CFG="${DEV_ROOT}/.yamllint"
export DEV_LIB_YAMLLINT_CFG
