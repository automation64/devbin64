# delete-me # Add to the CICD components section of the dot.env file

# Lint / YAML / Target
DEV_LINT_YAMLLINT_TARGET="$DEV_PATH_SRC"
export DEV_LINT_YAMLLINT_TARGET

# Lint / YAML / Configuration
DEV_LIB_YAMLLINT_CFG="${DEV_PATH_ROOT}/.yamllint"
export DEV_LIB_YAMLLINT_CFG
