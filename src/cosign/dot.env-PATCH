# delete-me # Add to the CICD components section of the dot.env file

# Auth / Cosign / Registry
DEV_AUTH_COSIGN_SERVER='X_REGISTRY_X'
export DEV_AUTH_COSIGN_SERVER

# Auth / Cosign / Registry Type
DEV_AUTH_COSIGN_TYPE='GITHUB'
export DEV_AUTH_COSIGN_TYPE
