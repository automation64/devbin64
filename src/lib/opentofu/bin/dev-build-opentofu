#!/usr/bin/env bash
# * template-task: 1.8.0
#######################################
# Copyright SerDigital64 - https://github.com/serdigital64
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at: http://www.apache.org/licenses/LICENSE-2.0
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#######################################

# shellcheck source=bin/dev-set
source ./bin/dev-set || { echo 'dev-set:Error: unable to load dev environment' 2>&1 && exit 1; }
# shellcheck source-path=bin
source ./bin/dev-env-gitops &&
  source ./bin/dev-env-opentofu || exit 1
# shellcheck source-path=lib/bl64
source "${DEV_LIB_BASHLIB64_TARGET}/bashlib64-module-txt.bash" &&
  source "${DEV_LIB_BASHLIB64_TARGET}/bashlib64-module-fmt.bash" &&
  source "${DEV_LIB_BASHLIB64_TARGET}/bashlib64-module-fs.bash" &&
  source "${DEV_LIB_BASHLIB64_TARGET}/bashlib64-core.bash" || exit 1

declare MY_MODULE="${1:-}"
declare MY_VERSION="${2:-}"
declare MY_STAGING=''
declare MY_SOURCE=''
declare MY_CONFIG=''

function my_build() {
  bl64_dbg_app_show_function
  bl64_fs_path_remove "$MY_STAGING"

  bl64_msg_show_task 'Create staging environment'
  bl64_fs_run_mkdir -p "$MY_STAGING" &&
    bl64_msg_show_task 'Promote source code to staging' &&
    bl64_fs_run_cp "$MY_SOURCE"/* "$MY_STAGING" ||
    return $?

  if [[ -d "$MY_CONFIG" ]]; then
    bl64_msg_show_task 'Promote configuration files to staging'
    bl64_fs_run_cp "$MY_CONFIG"/* "$MY_STAGING"
  fi
}

function my_initialize() {
  bl64_dbg_app_show_function
  bl64_msg_show_about

  if [[ "$DEV_ENV_GITOPS_ENABLED" == 'YES' ]]; then
    db64_profile_check_set &&
      bl64_check_parameter 'MY_MODULE' &&
      bl64_check_parameter 'MY_VERSION' ||
      return $?

    MY_SOURCE="${DEV_PATH_SRC}/${MY_MODULE}/${DEV_ENV_GITOPS_TAG_OPENTOFU}/${MY_VERSION}"
    MY_STAGING="${DEV_PATH_PROF_BUILD}/${DEV_ENV_GITOPS_TAG_OPENTOFU}/${MY_MODULE}/${MY_VERSION}"
    MY_CONFIG="${DEV_PATH_PROF_ETC}/${MY_MODULE}/${DEV_ENV_GITOPS_TAG_OPENTOFU}/${MY_VERSION}"
  else
    db64_profile_check_disabled || return $?
    MY_SOURCE="${DEV_PATH_SRC}"
    MY_STAGING="${DEV_PATH_BUILD}/${DEV_ENV_GITOPS_TAG_OPENTOFU}"
  fi

  bl64_check_command "$DEV_LIB_OPENTOFU_BIN" &&
    bl64_check_directory "$MY_SOURCE" &&
    bl64_msg_show_setup "$BL64_VAR_DEFAULT" \
      'DEV_ENV_GITOPS_ENABLED' \
      'DEV_PROFILE' \
      'MY_MODULE' \
      'MY_VERSION' \
      'MY_SOURCE' \
      'MY_STAGING'
}

[[ -n "$DEV_CICD_DEBUG" ]] && bl64_dbg_all_enable
bl64_lib_script_version_set '1.0.0'
bl64_msg_help_about_set 'Create runtime environment from source module'
bl64_msg_all_enable_verbose && my_initialize || exit $?

bl64_msg_show_batch_start "$BL64_SCRIPT_ID"
my_build
bl64_msg_show_batch_finish $? "$BL64_SCRIPT_ID"
