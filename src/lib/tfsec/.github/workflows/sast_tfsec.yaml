---
# Template: 1.1.0
name: "SAST / TFSec"

env:
  DEV_CICD_DEBUG: ""
  BL64_LIB_CICD: "YES"
  DEV_TEST_BASH_RUNNER_ENVIRONMENT: "ON"

on:
  pull_request:
    branches:
      - "main"
  push:
    branches:
      - "feature/**"
    paths:
      - "src/**"

jobs:
  sast-tfsec:
    runs-on: "ubuntu-latest"
    steps:

      - name: "Check out repository code"
        uses: "actions/checkout@main"

      - name: "Prepare environment"
        run: "./bin/dev-lib-base"

      - name: "Download tools"
        run: "./bin/dev-lib-tfsec"

      - name: "Run Lint"
        run: "./bin/dev-lint-tfsec"
...
